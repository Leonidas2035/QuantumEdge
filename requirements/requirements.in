# Core runtime dependencies shared across the monorepo.
# Keep this list minimal and derived from actual imports.

pyyaml>=6.0
requests
websockets>=11.0
numpy
pandas
python-binance
xgboost
cryptography>=42.0.0
openai>=1.30
