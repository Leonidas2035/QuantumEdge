Останні зміни від 22 червня 2026 року:

1. Рефакторинг баз даних та уніфіковані схеми QuestDB (Етап 1):
   - Замінено окремі таблиці для кожного бота на три єдині доменні таблиці: `market_features`, `bot_telemetry` та `llm_decisions`.
   - Всі таблиці партиційовані за днями (`PARTITION BY DAY`) для ефективного керування дисковим простором.
   - Створено автоматизований Python-скрипт для очищення застарілих партицій (`ALTER TABLE ... DROP PARTITION`) за політикою збереження (retention policy).

2. Оновлення шару завантаження даних та ILP Ingestion (Етап 2):
   - Інтегровано протокол Influx Line Protocol (ILP) на порт 9009 через бібліотеку `questdb` для високошвидкісного асинхронного запису фіч (`market_features`) та телеметрії ботів (`bot_telemetry`).
   - Додано фоновий неблокуючий воркер (queue worker) для запобігання затримкам у гарячому циклі HFT-ботів.

3. Структурований вивід рішень Hermes (Gemini 2.0):
   - Переведено `GoogleGeminiBackend` на новий `google-genai` SDK та використання Pydantic-моделей для жорсткого контролю JSON-схем (`DecisionV1Schema`).
   - Результати ризикових рішень Hermes автоматично записуються в реальному часі у таблицю `llm_decisions` через QuestDB REST API (порт 9000).

4. Оновлення та виправлення тестів:
   - Всі тести в `src/quantum_edge_ml/model_router/tests` переведено на асинхронне виконання (`async def` + `@pytest.mark.asyncio`).
   - Виправлено моки для клієнта `Client.aio.models.generate_content`.
   - Додано fallback-ключ `MOCK_KEY` для ініціалізації клієнта за відсутності змінних середовища API-ключа, що гарантує 100% проходження тестів (31 пройдено, 1 пропущено).

5. Виправлення ZMQ Heartbeat у HFT-ботах:
   - Додано безперервне надсилання статусних повідомлень (heartbeat) у фоновому циклі `ai_scalper_bot`, завдяки чому `zmq_mcp_bridge` стабільно визначає статус бота як `RUNNING` (online).
   - Оброблено потенційні мікро-дисконекти вебсокета BingX.

6. QuestDB Retrieval Layer & Bot Warm-up (Етап 3):
   - Реалізовано асинхронний збирач історичних даних `QuestDBQueryBuilder` у `src/quantum_edge_core/market_data/tsdb/query_builder.py` через REST API (порт 9000).
   - Інтегровано послідовність прогріву (`warm_up()`) для торгових ботів: `ai_scalper_bot` (мікроструктура за останні 15 хвилин), `dyndca` (профіль волатильності за останні 4 години) та `lockbotbtc` (VWAP-стрічки за 1 день).
   - Гарантовано, що боти підключаються до живих ZMQ-потоків `MarketDataHub` тільки після завершення прогріву, що запобігає аномаліям на старті.

7. Аналітика, MCP Tools та Dashboard API (Етап 4):
   - Розширено `data_mcp_bridge.py` додатковими CLI-командами `query_telemetry` та `query_market_trend`. Вони виконують агреговані запити `SAMPLE BY` до QuestDB для передачі LLM стислої інформації без ризику переповнення токенів.
   - Додано нові GET кінцеві точки `/api/v1/charts/features` та `/api/v1/charts/pnl` у `dashboard_api.py` для асинхронного фетчингу даних з QuestDB.
   - Дані автоматично перетворюються та оптимізуються безпосередньо у QuestDB через команду `to_unix_timestamp(ts) / 1000000` для повної сумісності з бібліотекою TradingView Lightweight Charts на фронтенді.
   - Реалізовано повне тестування для нових аналітичних інтерфейсів.