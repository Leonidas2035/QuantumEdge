supervisor_path: "/home/korben/.hermes/hermes/supervisor.py"
hub_path: "src/quantum_edge_core/market_data/hub.py"
bot_path: "src/quantum_edge_core/ai_scalper_bot/run_bot.py"

env_vars:
  MARKET_DATA_MODE: "live"
  MARKET_DATA_SYMBOLS: "BTCUSDT"
  MARKET_DATA_ZMQ_PORT: "5555"
  BINANCE_WS_URL: "wss://stream.binancefuture.com/ws"
  BINANCE_TESTNET: "True"
  MARKET_DATA_LOCKBOT_ENABLED: "0" # Disable to prevent crash due to missing publisher
  # Keys should be set in environment:
  # BINANCE_API_KEY=...
  # BINANCE_API_SECRET=...
