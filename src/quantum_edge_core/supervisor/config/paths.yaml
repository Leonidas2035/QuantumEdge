# Paths configuration for SupervisorAgent.
# Adjust these paths to match your environment.

quantumedge_root: "src/quantum_edge_core/ai_scalper_bot"
python_executable: ""  # leave empty to use the current Python interpreter
meta_agent_root: "src/quantum_edge_infra/automation/meta_agent"
logs_dir: "logs"
runtime_dir: "runtime"
events_dir: ""  # optional, defaults to <logs_dir>/events
reports_dir: ""  # optional, defaults to <project_root>/reports
